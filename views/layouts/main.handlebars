<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tech Blog</title>
    <link rel="stylesheet" href="./css/reset.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootswatch/4.1.3/flatly/bootstrap.min.css" />
    <link rel="stylesheet" href="./css/style.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Anton&family=Signika+Negative:wght@500&display=swap"
        rel="stylesheet">
</head>

<body class="container-fluid">
    <header class="mx-auto">
        <nav class="nav">
            <ul class="row nav ml-3">
                {{#if logged_in}}
                <li class="nav-item">
                    <a class="nav-link button" href="/" id="home-button">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link button" href="/dashboard" id="dashboard-button">Dashboard</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link button" id="logout">Logout</a>
                </li>
                {{else}}
                <li class="nav-item">
                    <a class="nav-link button" href="/login" id="login-button">Login</a>
                </li>
                {{/if}}
            </ul>
        </nav>
        <h1>
            The Tech Blog
        </h1>
    </header>
    <main class="row">

        {{{body}}}

    </main>
    {{#if logged_in}}
    <script src="./js/logout.js"></script>
    <script src="./js/comment.js"></script>
    <script src="./js/edit.js"></script>
    {{/if}}
</body>

</html>